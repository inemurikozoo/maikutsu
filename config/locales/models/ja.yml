ja:
  activerecord:
    models:
      user: ユーザー
    attributes:
      user:
        name: 名前
        email: メールアドレス
    models:
      sub_item: こもの
    attributes:
      sub_item:
        subname: こものの名前
        unit: 単位
        item_id: もの
        inventory: 在庫数
        inv_constant: 在庫アラート
        memo: メモ