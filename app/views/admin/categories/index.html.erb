<style>
  #Button{
    width: 120px;
    height: 50px;
    text-align: center;
    font-size: 15px;
    font-weight: bold;
    line-height: 50px;
    border: solid 1px #000000;
    border-radius: 10px;
    word-wrap: break-word;
    box-sizing: border-box;
  }
</style>

<div class="container">
  <h1 class="mt-3 text-center bg-success border border-secondary">カテゴリ一覧</h1>
  <div class="col-10-md">

    <% @categories.each do |category| %>
      <div class="table-responsive">
        <table class="table">
          <thead>
            <tr>
              <th>
                カテゴリID
              </th>
              <th>
                カテゴリ表示
              </th>
              <th>
                ボタンの色
              </th>
              <th>
              </th>
            </tr>
            <tr>
              <td>
                <%= category.id %>
              </td>
              <td>
                <div class="Button">
                  <%= category.name %>
                </div>
              </td>
              <td>
                <input type="hidden" id="colorCode" class="colorCode" value="<%= @colorCode %>">
                  <%= category.color_code %>
                </div>
              </td>
              <td>
                <%= link_to "編集する", edit_admin_category_path(category.id), class:"btn btn-success" %>
              </td>
            </tr>
          </thead>
        </table>
    <% end %>
    <script>
      window.onload = function(){
        const colorCode = document.getElementByClassName('colorCode').value;
        document.getElementById('Button').style.backgroundColor = colorCode;
      }
    </script>
    
    
      </div>
  </div>
</div>