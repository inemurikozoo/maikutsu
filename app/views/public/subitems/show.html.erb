<div class="container">
  <h1 class="mt-3 text-center bg-success border border-secondary">こもの詳細</h1>
    <div class="col-10-md">
    <%= form_with model: @sub_item, url: ,local: true do |f| %>
    <div class="mono_image">
      <%=  %>
    </div>
    <h5 class="mt-3 text-center bg-right border border-secondary">
      <%= @sub_item.subname %>
    </h5>

      <%= @sub_item.category.name %>

    <div class="" id=>
      <p>いまの在庫</p>
      
      <h3><%= f.text_field :inventory, style: "border: none" %></h3>
      <p><%= @sub_item.unit %></p>
      
    </div>
    <div class="stock_alert">
      <p>在庫アラート</p>
    <% end %>
    </div>
  </div>
  <script>
    
  </script>
</div>
