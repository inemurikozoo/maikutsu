<div class="container">
  <h1 class="mt-3 text-center bg-success border border-secondary">こもの詳細</h1>
    <%= render 'layouts/genre_side_menu', genres: @genres %>
  <div class="col-10-md">
    <%= form_with model: @sub_item, local: true do |f| %>
    <div class="mono_image">
      <%= image_tag %>
    </div>
    <h5 class="mt-3 text-center bg-right border border-secondary">
      <%= @item.name %>
    </h5>

      <%= @item.genre.name %>

    <div class="now_stock">
      <p>いまの在庫</p>
      <div class=""０ 
      <h3><%= @sub_item.inventory %></h3>
      <p><%= @sub_item.unit %></p>
      
    </div>
    <div class="stock_alert">
      <p>在庫アラート</p>
      
    </div>
  </div>
  <script>
    
  </script>
</div>
